---

---

<nav
  id="navbar"
  class="fixed inset-x-0 top-0 w-full py-4 px-6 md:px-12 flex justify-between items-center z-[2147483647] transition-all duration-300"
  data-aos="fade-down"
  data-aos-delay="100"
  data-aos-once="true"
>
  <div
    class="flex items-center rounded-2xl p-2"
    data-aos="fade-right"
    data-aos-delay="300"
    data-aos-once="true"
  >
    <img src="/icons/logo.svg" alt="Logo" class="h-10 md:h-16" />
  </div>

  <!-- Navigation Links (hidden on mobile, shown on desktop) -->
  <div
    class="hidden lg:flex items-center space-x-8 text-xl"
    data-aos="fade-down"
    data-aos-delay="500"
    data-aos-once="true"
  >
    <a
      href="#inicio"
      class="text-white hover:text-primary font-medium transition-colors duration-300"
      data-aos="fade-down"
      data-aos-delay="600"
      data-aos-once="true"
    >
      Inicio
    </a>
    <a
      href="#avances"
      class="text-white hover:text-primary font-medium transition-colors duration-300"
      data-aos="fade-down"
      data-aos-delay="700"
      data-aos-once="true"
    >
      Avances
    </a>
    <a
      href="#atributos"
      class="text-white hover:text-primary font-medium transition-colors duration-300"
      data-aos="fade-down"
      data-aos-delay="800"
      data-aos-once="true"
    >
      Atributos
    </a>
    <a
      href="#contacto"
      class="text-white hover:text-primary font-medium transition-colors duration-300"
      data-aos="fade-down"
      data-aos-delay="900"
      data-aos-once="true"
    >
      Contacto
    </a>
  </div>

  <a
    href="#contacto"
    class="bg-gradient-to-t from-[#e98f0d] to-primary shadow-2xl hover:bg-primary/90 text-white lg:text-lg font-semibold lg:font-bold px-5 lg:px-8 py-1 lg:py-1.5 rounded-full transition-colors duration-300"
    data-aos="fade-left"
    data-aos-delay="700"
    data-aos-once="true"
  >
    SEPARA CON S/1000
  </a>
</nav>

<style>
  #navbar {
    position: fixed !important;
    z-index: 2147483647 !important;
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const navbar = document.getElementById("navbar");

    function updateNavbarStyle() {
      const scrolled = window.scrollY > 50;

      if (scrolled) {
        navbar?.classList.add("backdrop-blur-md", "bg-secondary/40");
        navbar?.classList.remove("bg-transparent");
      } else {
        navbar?.classList.remove("backdrop-blur-md", "bg-secondary/40");
        navbar?.classList.add("bg-transparent");
      }
    }

    // Initial state
    updateNavbarStyle();

    // Update on scroll
    window.addEventListener("scroll", updateNavbarStyle);

    // Smooth scroll for navigation links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener("click", (e) => {
        e.preventDefault();
        const el = e.currentTarget as HTMLAnchorElement | null;
        const href = el?.getAttribute("href") || "";
        const target = href ? document.querySelector(href) : null;
        if (target) {
          target.scrollIntoView({
            behavior: "smooth",
            block: "start",
          });
        }
      });
    });
  });
</script>
